# Description
Let installed NuGet packages be managed by [`Paket`](https://github.com/fsprojects/Paket) instead of `.NET` ecosystem

It is done by converting `Directory.Packages.props` to the file `Paket` needed (through directly modifying file content and renaming file)
# Features